import styled from "styled-components"

const StyledMenuContact = styled.div`
  position: absolute;
  bottom: 0;
  display: flex;
  @media (max-width: 968px) {
    display: none;
  }
`

export default StyledMenuContact
